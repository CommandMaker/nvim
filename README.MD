# Neovim minimal config

The goal of this config is to make the lightest config using less plugins and using more native functionnalities. For example, replacing the `nvim-cmp` plugin by `omnicomplete`, the native Neovim completion feature.
